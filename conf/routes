# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST    /auth/login                 controller.AuthenticatorController.login

POST    /profiles                   controller.ProfileController.create
GET     /profiles/                  controller.ProfileController.read()
GET     /profiles/:username         controller.ProfileController.read(username: String)